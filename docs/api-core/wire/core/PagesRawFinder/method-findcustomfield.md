# $pagesRawFinder->findCustomField(Field $field, array $cols)

Source: `wire/core/PagesRaw.php`

Find raw custom field

## Usage

~~~~~
// basic usage
$result = $pagesRawFinder->findCustomField($field, $cols);

// usage with all arguments
$result = $pagesRawFinder->findCustomField(Field $field, array $cols);
~~~~~

## Arguments

- `$field` `Field`
- `$cols` `array`

## Exceptions

- `WireException`
