# $wire->permissions($selector = ''): Permissions|Permission|PageArray|null|NullPage

Source: `wire/core/Wire.php`

Access the $permissions API variable as a function.

## Usage

~~~~~
// basic usage
$permissions = $wire->permissions();

// usage with all arguments
$permissions = $wire->permissions($selector = '');
~~~~~
