# $wire->users($selector = ''): Users|PageArray|User|mixed

Source: `wire/core/Wire.php`

Access the $users API variable as a function.

## Usage

~~~~~
// basic usage
$users = $wire->users();

// usage with all arguments
$users = $wire->users($selector = '');
~~~~~
