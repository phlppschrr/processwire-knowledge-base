# $wire->pages($selector = ''): Pages|PageArray|Page|NullPage

Source: `wire/core/Wire.php`

Access the $pages API variable as a function.

## Usage

~~~~~
// basic usage
$pages = $wire->pages();

// usage with all arguments
$pages = $wire->pages($selector = '');
~~~~~
