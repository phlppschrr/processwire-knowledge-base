# $wire->roles($selector = ''): Roles|Role|PageArray|null|NullPage

Source: `wire/core/Wire.php`

Access the $roles API variable as a function.

## Usage

~~~~~
// basic usage
$roles = $wire->roles();

// usage with all arguments
$roles = $wire->roles($selector = '');
~~~~~
