# $inputfield->head($head = null): string|Inputfield

Source: `wire/core/Inputfield.php`

Get or set the 'head' property via method. @since 3.0.110

## Usage

~~~~~
// basic usage
$string = $inputfield->head();

// usage with all arguments
$string = $inputfield->head($head = null);
~~~~~
