# $inputfield->requiredIf($requiredIf = null): string|Inputfield

Source: `wire/core/Inputfield.php`

Get or set required-if selector. @since 3.0.110

## Usage

~~~~~
// basic usage
$string = $inputfield->requiredIf();

// usage with all arguments
$string = $inputfield->requiredIf($requiredIf = null);
~~~~~
