# $notices->storeNotice(Notice $item): bool

Source: `wire/core/Notices.php`

Store a persist Notice in Session

## Usage

~~~~~
// basic usage
$bool = $notices->storeNotice($item);

// usage with all arguments
$bool = $notices->storeNotice(Notice $item);
~~~~~

## Arguments

- `$item` `Notice`

## Return value

- `bool`
