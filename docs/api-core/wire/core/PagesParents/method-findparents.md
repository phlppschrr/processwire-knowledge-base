# $pagesParents->findParents(array $options = array()): array|PageArray

Source: `wire/core/PagesParents.php`

Find all pages that have children

## Usage

~~~~~
// basic usage
$array = $pagesParents->findParents();

// usage with all arguments
$array = $pagesParents->findParents(array $options = array());
~~~~~

## Arguments

- `$options` (optional) `array`

## Return value

- `array|PageArray`

## Since

3.0.156
