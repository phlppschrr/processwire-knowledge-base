# $pageimageVariations->removeExtras(Pageimage $pageimage, array &$deletedFiles, array $options)

Source: `wire/core/PageimageVariations.php`

Remove extras

## Usage

~~~~~
// basic usage
$result = $pageimageVariations->removeExtras($pageimage, $deletedFiles, $options);

// usage with all arguments
$result = $pageimageVariations->removeExtras(Pageimage $pageimage, array &$deletedFiles, array $options);
~~~~~

## Arguments

- `$pageimage` `Pageimage`
- `$deletedFiles` `array`
- `$options` `array` See options for remove() method
