# $fieldgroupsArray->getItemKey($item)

Source: `wire/core/FieldgroupsArray.php`

Per WireArray interface, items are keyed by their ID

## Usage

~~~~~
// basic usage
$result = $fieldgroupsArray->getItemKey($item);
~~~~~
