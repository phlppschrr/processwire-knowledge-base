# $users->new($options = []): User

Source: `wire/core/Users.php`

Create new User instance in memory (3.0.249+)

## Usage

~~~~~
// basic usage
$user = $users->new();

// usage with all arguments
$user = $users->new($options = []);
~~~~~
