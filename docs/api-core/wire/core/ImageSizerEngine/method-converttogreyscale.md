# $imageSizerEngine->convertToGreyscale($dstFilename = ''): bool

Source: `wire/core/ImageSizerEngine.php`

Convert image to greyscale

## Usage

~~~~~
// basic usage
$bool = $imageSizerEngine->convertToGreyscale();

// usage with all arguments
$bool = $imageSizerEngine->convertToGreyscale($dstFilename = '');
~~~~~

## Arguments

- `$dstFilename` (optional) `string` If different from source file

## Return value

- `bool`
