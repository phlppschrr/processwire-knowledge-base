# $wireInputData->__construct(&$input = array(), $lazy = false)

Source: `wire/core/WireInputData.php`

Construct

## Usage

~~~~~
// basic usage
$result = $wireInputData->__construct();

// usage with all arguments
$result = $wireInputData->__construct(&$input = array(), $lazy = false);
~~~~~

## Arguments

- `$input` (optional) `array` Associative array of variables to store
- `$lazy` (optional) `bool` Use lazy loading?
