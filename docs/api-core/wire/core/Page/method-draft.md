# $page->draft($key = null, $value = null): ProDraft|int|string|Page|array

Source: `wire/core/Page.php`

Helper method for drafts (added by ProDrafts).

## Usage

~~~~~
// basic usage
$proDraft = $page->draft();

// usage with all arguments
$proDraft = $page->draft($key = null, $value = null);
~~~~~
