# $adminTheme->addExtraMarkup($name, $value)

Source: `wire/core/AdminTheme.php`

Add extra markup to a region in the admin theme

## Usage

~~~~~
// basic usage
$result = $adminTheme->addExtraMarkup($name, $value);
~~~~~

## Arguments

- `$name` `string`
- `$value` `string`
