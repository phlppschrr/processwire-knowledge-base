# $fieldsArray->getItemKey($item): int

Source: `wire/core/FieldsArray.php`

Per WireArray interface, Field instances are keyed by their ID

## Usage

~~~~~
// basic usage
$int = $fieldsArray->getItemKey($item);
~~~~~

## Arguments

- `$item` `Field`

## Return value

- `int`
