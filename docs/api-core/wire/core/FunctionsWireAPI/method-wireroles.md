# $functionsWireAPI->wireRoles($selector = ''): Roles|Role|PageArray|null|NullPage

Source: `wire/core/FunctionsWireAPI.php`

Access the $roles API varaible as a function

See the pages() function for usage details.

## Usage

~~~~~
// basic usage
$roles = $functionsWireAPI->wireRoles();

// usage with all arguments
$roles = $functionsWireAPI->wireRoles($selector = '');
~~~~~

## Arguments

- `$selector` (optional) `string`

## Return value

- `Roles|Role|PageArray|null|NullPage`
