# $inputfieldWrapper->append(Inputfield $item): $this

Source: `wire/core/InputfieldWrapper.php`

Append an Inputfield to this instance’s children.

## Usage

~~~~~
// basic usage
$result = $inputfieldWrapper->append($item);

// usage with all arguments
$result = $inputfieldWrapper->append(Inputfield $item);
~~~~~

## Arguments

- `$item` `Inputfield` Item to append

## Return value

- `$this`
