# $pageAction->action(Page $item): bool

Source: `wire/core/PageAction.php`

Declared in class docblock.

## Usage

~~~~~
// basic usage
$bool = $pageAction->action($item);

// usage with all arguments
$bool = $pageAction->action(Page $item);
~~~~~
