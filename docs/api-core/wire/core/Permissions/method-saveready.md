# $permissions->saveReady(Page $page): array

Source: `wire/core/Permissions.php`

Hook called just before a Permission is saved

## Usage

~~~~~
// basic usage
$array = $permissions->saveReady($page);

// usage with all arguments
$array = $permissions->saveReady(Page $page);
~~~~~
