# $permissions->added(Page $page): void

Source: `wire/core/Permissions.php`

Hook called just after a Permission is added

## Usage

~~~~~
// basic usage
$permissions->added($page);

// usage with all arguments
$permissions->added(Page $page);
~~~~~
