# $permissions->find($selector): PageArray

Source: `wire/core/Permissions.php`

Return the permissions(s) matching the the given selector query.

## Usage

~~~~~
// basic usage
$items = $permissions->find($selector);
~~~~~
