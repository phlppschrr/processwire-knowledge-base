# $permissions->deleteReady(Page $page): void

Source: `wire/core/Permissions.php`

Hook called before a Permission is deleted

## Usage

~~~~~
// basic usage
$permissions->deleteReady($page);

// usage with all arguments
$permissions->deleteReady(Page $page);
~~~~~
