# $permissions->new($options = []): Permission

Source: `wire/core/Permissions.php`

Create new Permission instance in memory (3.0.249+)

## Usage

~~~~~
// basic usage
$permission = $permissions->new();

// usage with all arguments
$permission = $permissions->new($options = []);
~~~~~
