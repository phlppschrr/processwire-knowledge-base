# $modules->__construct($path)

Source: `wire/core/Modules.php`

Construct the Modules

## Usage

~~~~~
// basic usage
$result = $modules->__construct($path);
~~~~~

## Arguments

- `$path` `string` Core modules path (you may add other paths with addPath method)
