# $wireMarkupFileRegions->applyRegionNamespace(array &$region)

Source: `wire/core/WireMarkupFileRegions.php`

Apply namespace to region content

## Usage

~~~~~
// basic usage
$result = $wireMarkupFileRegions->applyRegionNamespace($region);

// usage with all arguments
$result = $wireMarkupFileRegions->applyRegionNamespace(array &$region);
~~~~~

## Arguments

- `$region` `array`
