# $database->escapeLike($like): string

Source: `wire/core/Database.php`

Escape a string value, plus escape characters necessary for a MySQL 'LIKE' phrase

## Usage

~~~~~
// basic usage
$string = $database->escapeLike($like);
~~~~~

## Arguments

- `$like` `string`

## Return value

- `string`
