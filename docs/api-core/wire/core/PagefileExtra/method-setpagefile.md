# $pagefileExtra->setPagefile(Pagefile $pagefile)

Source: `wire/core/PagefileExtra.php`

Set Pagefile instance this extra is connected to

## Usage

~~~~~
// basic usage
$result = $pagefileExtra->setPagefile($pagefile);

// usage with all arguments
$result = $pagefileExtra->setPagefile(Pagefile $pagefile);
~~~~~

## Arguments

- `$pagefile` `Pagefile`
