# $markupQA->wakeupUrls(&$value)

Source: `wire/core/MarkupQA.php`

Wakeup URLs in href or src attributes for presentation

## Usage

~~~~~
// basic usage
$result = $markupQA->wakeupUrls($value);

// usage with all arguments
$result = $markupQA->wakeupUrls(&$value);
~~~~~

## Arguments

- $value
