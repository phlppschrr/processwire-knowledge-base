# $markupQA->setPage(Page $page)

Source: `wire/core/MarkupQA.php`

Set the current Page

## Usage

~~~~~
// basic usage
$result = $markupQA->setPage($page);

// usage with all arguments
$result = $markupQA->setPage(Page $page);
~~~~~

## Arguments

- `$page` `Page`
