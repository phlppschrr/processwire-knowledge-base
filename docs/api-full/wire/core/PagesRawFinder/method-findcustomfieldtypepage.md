# $pagesRawFinder->findCustomFieldtypePage(Field $field, $fieldName, array $pageRefCols)

Source: `wire/core/PagesRaw.php`

Find and apply values for Page reference fields

## Usage

~~~~~
// basic usage
$result = $pagesRawFinder->findCustomFieldtypePage($field, $fieldName, $pageRefCols);

// usage with all arguments
$result = $pagesRawFinder->findCustomFieldtypePage(Field $field, $fieldName, array $pageRefCols);
~~~~~

## Arguments

- `$field` `Field`
- `$fieldName` `string`
- `$pageRefCols` `array`
