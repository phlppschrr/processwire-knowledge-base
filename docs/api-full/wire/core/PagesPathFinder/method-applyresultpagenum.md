# $pagesPathFinder->applyResultPageNum(array &$parts)

Source: `wire/core/PagesPathFinder.php`

Identify and populate pagination number from $result['urlSegments']

## Usage

~~~~~
// basic usage
$result = $pagesPathFinder->applyResultPageNum($parts);

// usage with all arguments
$result = $pagesPathFinder->applyResultPageNum(array &$parts);
~~~~~

## Arguments

- `$parts` `array`
