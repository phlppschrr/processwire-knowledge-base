# $fieldtypeMulti->lockForWriting(Field $field): bool

Source: `wire/core/FieldtypeMulti.php`

Lock field table for writing

## Usage

~~~~~
// basic usage
$bool = $fieldtypeMulti->lockForWriting($field);

// usage with all arguments
$bool = $fieldtypeMulti->lockForWriting(Field $field);
~~~~~

## Arguments

- `$field` `Field`

## Return value

- `bool`
