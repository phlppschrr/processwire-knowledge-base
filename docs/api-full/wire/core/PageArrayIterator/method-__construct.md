# $pageArrayIterator->__construct(array $lazypages, array $options = [])

Source: `wire/core/PageArrayIterator.php`

Construct

## Usage

~~~~~
// basic usage
$result = $pageArrayIterator->__construct($lazypages);

// usage with all arguments
$result = $pageArrayIterator->__construct(array $lazypages, array $options = []);
~~~~~

## Arguments

- `$lazypages` `array`
- `$options` (optional) `array` Options provided to $pages->find()
