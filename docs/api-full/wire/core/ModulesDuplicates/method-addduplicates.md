# $modulesDuplicates->addDuplicates($className, array $files)

Source: `wire/core/ModulesDuplicates.php`

Add multiple duplicates

## Usage

~~~~~
// basic usage
$result = $modulesDuplicates->addDuplicates($className, $files);

// usage with all arguments
$result = $modulesDuplicates->addDuplicates($className, array $files);
~~~~~

## Arguments

- $className
- `$files` `array`
