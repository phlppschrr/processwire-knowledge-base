# $page->sortable(): bool

Source: `wire/core/Page.php`

Returns true if the current user can change the sort order of the current page (within the same parent).

## Usage

~~~~~
// basic usage
$bool = $page->sortable();
~~~~~
