# $pagefiles->setField(Field $field)

Source: `wire/core/Pagefiles.php`

Set the field these files are assigned to

## Usage

~~~~~
// basic usage
$result = $pagefiles->setField($field);

// usage with all arguments
$result = $pagefiles->setField(Field $field);
~~~~~

## Arguments

- `$field` `Field`
