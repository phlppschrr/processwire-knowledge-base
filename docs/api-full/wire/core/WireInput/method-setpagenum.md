# $wireInput->setPageNum($num)

Source: `wire/core/WireInput.php`

Set the current page number.

- This is typically used only by the core.
- Note that the first page should be 1 (not 0).

## Usage

~~~~~
// basic usage
$result = $wireInput->setPageNum($num);
~~~~~

## Arguments

- `$num` `int`
