# $pagesAccess->deletePage(Page $page)

Source: `wire/core/PagesAccess.php`

Delete a page from the pages_access table

## Usage

~~~~~
// basic usage
$result = $pagesAccess->deletePage($page);

// usage with all arguments
$result = $pagesAccess->deletePage(Page $page);
~~~~~

## Arguments

- `$page` `Page`
