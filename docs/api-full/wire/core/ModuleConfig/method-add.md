# $moduleConfig->add(array $a): self

Source: `wire/core/ModuleConfig.php`

Set an array that defines Inputfields

## Usage

~~~~~
// basic usage
$result = $moduleConfig->add($a);

// usage with all arguments
$result = $moduleConfig->add(array $a);
~~~~~

## Arguments

- `$a` `array`

## Return value

- `self`
