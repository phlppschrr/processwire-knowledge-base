# $pagefilesManager->__construct(Page $page)

Source: `wire/core/PagefilesManager.php`

Construct the PagefilesManager and ensure all needed paths are created

## Usage

~~~~~
// basic usage
$result = $pagefilesManager->__construct($page);

// usage with all arguments
$result = $pagefilesManager->__construct(Page $page);
~~~~~

## Arguments

- `$page` `Page`
