# $wire->user($key = '', $value = null): User|mixed

Source: `wire/core/Wire.php`

Access the $user API variable as a function.

## Usage

~~~~~
// basic usage
$user = $wire->user();

// usage with all arguments
$user = $wire->user($key = '', $value = null);
~~~~~
