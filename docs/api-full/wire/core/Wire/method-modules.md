# $wire->modules($name = ''): Modules|Module|ConfigurableModule|null

Source: `wire/core/Wire.php`

Access the $modules API variable as a function.

## Usage

~~~~~
// basic usage
$modules = $wire->modules();

// usage with all arguments
$modules = $wire->modules($name = '');
~~~~~
