# $pagefile->getNext(): Pagefile|null

Source: `wire/core/Pagefile.php`

Return the next sibling Pagefile in the parent Pagefiles, or NULL if at the end.

## Usage

~~~~~
// basic usage
$pagefile = $pagefile->getNext();
~~~~~

## Return value

- `Pagefile|null`
