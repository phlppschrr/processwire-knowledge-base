# $users->saved($user, array $changes): void

Source: `wire/core/Users.php`

Hook called after a User has been saved

## Usage

~~~~~
// basic usage
$users->saved($user, $changes);

// usage with all arguments
$users->saved($user, array $changes);
~~~~~
