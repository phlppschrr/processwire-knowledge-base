# $users->setCurrentUser(User $user)

Source: `wire/core/Users.php`

Set the current system user (the $user API variable)

## Usage

~~~~~
// basic usage
$result = $users->setCurrentUser($user);

// usage with all arguments
$result = $users->setCurrentUser(User $user);
~~~~~

## Arguments

- `$user` `User`
