# $wireSessionHandler->hookSessionInit(HookEvent $event)

Source: `wire/core/WireSessionHandler.php`

Hook before Session::init

## Usage

~~~~~
// basic usage
$result = $wireSessionHandler->hookSessionInit($event);

// usage with all arguments
$result = $wireSessionHandler->hookSessionInit(HookEvent $event);
~~~~~

## Arguments

- `$event` `HookEvent`
