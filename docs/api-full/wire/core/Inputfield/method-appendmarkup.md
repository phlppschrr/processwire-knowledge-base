# $inputfield->appendMarkup($markup = null): string|Inputfield

Source: `wire/core/Inputfield.php`

Get or set the 'appendMarkup' property via method. @since 3.0.110

## Usage

~~~~~
// basic usage
$string = $inputfield->appendMarkup();

// usage with all arguments
$string = $inputfield->appendMarkup($markup = null);
~~~~~
