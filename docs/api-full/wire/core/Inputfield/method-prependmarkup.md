# $inputfield->prependMarkup($markup = null): string|Inputfield

Source: `wire/core/Inputfield.php`

Get or set the 'prependMarkup' property via method. @since 3.0.110

## Usage

~~~~~
// basic usage
$string = $inputfield->prependMarkup();

// usage with all arguments
$string = $inputfield->prependMarkup($markup = null);
~~~~~
