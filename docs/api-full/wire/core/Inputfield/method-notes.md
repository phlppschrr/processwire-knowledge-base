# $inputfield->notes($notes = null): string|Inputfield

Source: `wire/core/Inputfield.php`

Get or set the 'notes' property via method. @since 3.0.110

## Usage

~~~~~
// basic usage
$string = $inputfield->notes();

// usage with all arguments
$string = $inputfield->notes($notes = null);
~~~~~
