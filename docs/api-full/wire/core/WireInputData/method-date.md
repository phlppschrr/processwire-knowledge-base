# $wireInputData->date($varName, $dateFormat): string

Source: `wire/core/WireInputData.php`

Validate and return $varName in the given PHP date() or strftime() format.

## Usage

~~~~~
// basic usage
$string = $wireInputData->date($varName, $dateFormat);
~~~~~
