# $wireInputData->textarea($varName): string

Source: `wire/core/WireInputData.php`

Sanitize to multi-line text up to 16k characters (48k bytes), HTML markup is removed

## Usage

~~~~~
// basic usage
$string = $wireInputData->textarea($varName);
~~~~~
