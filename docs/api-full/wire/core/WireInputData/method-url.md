# $wireInputData->url($varName): string

Source: `wire/core/WireInputData.php`

Sanitize to a valid URL, or convert to blank if it can't be sanitized

## Usage

~~~~~
// basic usage
$string = $wireInputData->url($varName);
~~~~~
