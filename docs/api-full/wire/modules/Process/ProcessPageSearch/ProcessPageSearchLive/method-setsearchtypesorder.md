# $processPageSearchLive->setSearchTypesOrder(array $types)

Source: `wire/modules/Process/ProcessPageSearch/ProcessPageSearchLive.php`

Set order of search types

## Usage

~~~~~
// basic usage
$result = $processPageSearchLive->setSearchTypesOrder($types);

// usage with all arguments
$result = $processPageSearchLive->setSearchTypesOrder(array $types);
~~~~~

## Arguments

- `$types` `array` Names of types, in order
