# $listerBookmarks->savePublicBookmarks(array $bookmarks): bool

Source: `wire/modules/Process/ProcessPageLister/ListerBookmarks.php`

Save public bookmarks (to module config)

## Usage

~~~~~
// basic usage
$bool = $listerBookmarks->savePublicBookmarks($bookmarks);

// usage with all arguments
$bool = $listerBookmarks->savePublicBookmarks(array $bookmarks);
~~~~~

## Arguments

- `$bookmarks` `array`

## Return value

- `bool`
