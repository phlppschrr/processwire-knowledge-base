# $listerBookmarks->setPage(Page $page)

Source: `wire/modules/Process/ProcessPageLister/ListerBookmarks.php`

Set the Lister page that bookmarks will be for

## Usage

~~~~~
// basic usage
$result = $listerBookmarks->setPage($page);

// usage with all arguments
$result = $listerBookmarks->setPage(Page $page);
~~~~~

## Arguments

- `$page` `Page`
