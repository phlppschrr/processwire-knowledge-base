# $processModuleInstall->canInstallFromFileUpload($notify = true): bool

Source: `wire/modules/Process/ProcessModule/ProcessModuleInstall.php`

Module upload allowed?

## Usage

~~~~~
// basic usage
$bool = $processModuleInstall->canInstallFromFileUpload();

// usage with all arguments
$bool = $processModuleInstall->canInstallFromFileUpload($notify = true);
~~~~~

## Arguments

- `$notify` (optional) `bool`

## Return value

- `bool`
