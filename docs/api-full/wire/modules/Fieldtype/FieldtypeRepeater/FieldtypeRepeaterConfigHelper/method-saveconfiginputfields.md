# $fieldtypeRepeaterConfigHelper->saveConfigInputfields(Template $template)

Source: `wire/modules/Fieldtype/FieldtypeRepeater/config.php`

Helper to getConfigInputfields, handles adding and removing of repeater fields

## Usage

~~~~~
// basic usage
$result = $fieldtypeRepeaterConfigHelper->saveConfigInputfields($template);

// usage with all arguments
$result = $fieldtypeRepeaterConfigHelper->saveConfigInputfields(Template $template);
~~~~~

## Arguments

- `$template` `Template`

## Exceptions

- `WireException`
