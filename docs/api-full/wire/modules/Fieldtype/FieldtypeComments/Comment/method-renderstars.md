# $comment->renderStars(array $options = array()): string

Source: `wire/modules/Fieldtype/FieldtypeComments/Comment.php`

Render stars markup

## Usage

~~~~~
// basic usage
$string = $comment->renderStars();

// usage with all arguments
$string = $comment->renderStars(array $options = array());
~~~~~

## Arguments

- `$options` (optional) `array` See CommentArray::renderStars for $options

## Return value

- `string`
