# $inputfieldTinyMCESettings->applyPlugins(array &$settings, array $defaults)

Source: `wire/modules/Inputfield/InputfieldTinyMCE/InputfieldTinyMCESettings.php`

Apply plugins settings

## Usage

~~~~~
// basic usage
$result = $inputfieldTinyMCESettings->applyPlugins($settings, $defaults);

// usage with all arguments
$result = $inputfieldTinyMCESettings->applyPlugins(array &$settings, array $defaults);
~~~~~

## Arguments

- `$settings` `array`
- `$defaults` `array`
