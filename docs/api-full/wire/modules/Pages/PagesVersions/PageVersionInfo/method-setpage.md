# $pageVersionInfo->setPage(Page $page)

Source: `wire/modules/Pages/PagesVersions/PageVersionInfo.php`

Set page that this version is for

## Usage

~~~~~
// basic usage
$result = $pageVersionInfo->setPage($page);

// usage with all arguments
$result = $pageVersionInfo->setPage(Page $page);
~~~~~

## Arguments

- `$page` `Page`
