# $wire->config($key = '', $value = null): Config|mixed

Source: `wire/core/Wire.php`

Access the $config API variable as a function.

## Usage

~~~~~
// basic usage
$config = $wire->config();

// usage with all arguments
$config = $wire->config($key = '', $value = null);
~~~~~
