# $wire->templates($name = ''): Templates|Template|null

Source: `wire/core/Wire.php`

Access the $templates API variable as a function.

## Usage

~~~~~
// basic usage
$templates = $wire->templates();

// usage with all arguments
$templates = $wire->templates($name = '');
~~~~~
