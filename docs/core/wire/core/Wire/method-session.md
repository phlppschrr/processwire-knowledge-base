# $wire->session($key = '', $value = null): Session|mixed

Source: `wire/core/Wire.php`

Access the $session API variable as a function.

## Usage

~~~~~
// basic usage
$session = $wire->session();

// usage with all arguments
$session = $wire->session($key = '', $value = null);
~~~~~
