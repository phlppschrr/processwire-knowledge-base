# $processWire->getHttpHost(Config $config): string

Source: `wire/core/ProcessWire.php`

Safely determine the HTTP host

## Usage

~~~~~
// basic usage
$string = $processWire->getHttpHost($config);

// usage with all arguments
$string = $processWire->getHttpHost(Config $config);
~~~~~

## Arguments

- `$config` `Config`

## Return value

- `string`
