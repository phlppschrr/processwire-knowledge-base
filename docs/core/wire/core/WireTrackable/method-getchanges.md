# $wireTrackable->getChanges($getValues = false): array

Source: `wire/core/Interfaces.php`

Return an array of properties that have changed while change tracking was on.

## Arguments

- bool $getValues If true, then an associative array will be retuned with field names and previous values.

## Return value

array
