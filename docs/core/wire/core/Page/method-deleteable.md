# $page->deleteable(): bool

Source: `wire/core/Page.php`

Returns true if the page is deleteable by the current user, false if not.

## Usage

~~~~~
// basic usage
$bool = $page->deleteable();
~~~~~
