# $wireInputData->purify($varName): string

Source: `wire/core/WireInputData.php`

Return a value run through HTML Purifier (value assumed to contain HTML)

## Usage

~~~~~
// basic usage
$string = $wireInputData->purify($varName);
~~~~~
