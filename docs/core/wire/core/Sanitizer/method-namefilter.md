# $sanitizer->nameFilter($value, array $allowedExtras, $replacementChar, $beautify = false, $maxLength = 128)

Source: `wire/core/Sanitizer.php`

**********************************************************************************************************
STRING SANITIZERS

## Usage

~~~~~
// basic usage
$result = $sanitizer->nameFilter($value, $allowedExtras, $replacementChar);

// usage with all arguments
$result = $sanitizer->nameFilter($value, array $allowedExtras, $replacementChar, $beautify = false, $maxLength = 128);
~~~~~
