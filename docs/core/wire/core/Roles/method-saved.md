# $roles->saved(Page $page, array $changes = [], $values = []): void

Source: `wire/core/Roles.php`

Hook called after a role has been saved

## Usage

~~~~~
// basic usage
$roles->saved($page);

// usage with all arguments
$roles->saved(Page $page, array $changes = [], $values = []);
~~~~~
