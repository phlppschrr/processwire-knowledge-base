# $roles->deleteReady(Page $page): void

Source: `wire/core/Roles.php`

Hook called before a Role is deleted

## Usage

~~~~~
// basic usage
$roles->deleteReady($page);

// usage with all arguments
$roles->deleteReady(Page $page);
~~~~~
