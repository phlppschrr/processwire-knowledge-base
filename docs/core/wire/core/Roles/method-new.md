# $roles->new($options = []): Role

Source: `wire/core/Roles.php`

Create new Role instance in memory (3.0.249+)

## Usage

~~~~~
// basic usage
$role = $roles->new();

// usage with all arguments
$role = $roles->new($options = []);
~~~~~
