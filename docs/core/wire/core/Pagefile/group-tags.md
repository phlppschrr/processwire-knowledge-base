# Pagefile: tags

Source: `wire/core/Pagefile.php`

Be sure to see the `Pagefiles::getTag()` and `Pagesfiles::findTag()` methods, which enable you retrieve files by tag.

- `$tagsArray: array` Get file tags as an array. @since 3.0.17
- [`$tags: string`](method-tags.md) Value of the file’s tags field (string), if enabled.
