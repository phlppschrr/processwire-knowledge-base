# $pagesRawFinder->findMeta(array $names)

Source: `wire/core/PagesRaw.php`

Populate 'meta' to (form pages_meta table) to the result values

## Usage

~~~~~
// basic usage
$result = $pagesRawFinder->findMeta($names);

// usage with all arguments
$result = $pagesRawFinder->findMeta(array $names);
~~~~~

## Arguments

- `$names` `array`

## Since

3.0.193
