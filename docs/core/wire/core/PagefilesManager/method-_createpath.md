# $pagefilesManager->_createPath($path): bool

Source: `wire/core/PagefilesManager.php`

Create a directory with proper permissions, for internal use.

## Arguments

- `$path` `string` Path to create

## Return value

bool True on success, false if not
