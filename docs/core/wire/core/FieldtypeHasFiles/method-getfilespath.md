# $fieldtypeHasFiles->getFilesPath(Page $page, Field $field): string

Source: `wire/core/Interfaces.php`

Get path where files are (or would be) stored

## Usage

~~~~~
// basic usage
$string = $fieldtypeHasFiles->getFilesPath($page, $field);

// usage with all arguments
$string = $fieldtypeHasFiles->getFilesPath(Page $page, Field $field);
~~~~~

## Arguments

- `$page` `Page`
- `$field` `Field`

## Return value

- `string`
