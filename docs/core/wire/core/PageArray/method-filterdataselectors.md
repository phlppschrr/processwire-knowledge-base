# $pageArray->filterDataSelectors(Selectors $selectors)

Source: `wire/core/PageArray.php`

Prepare selectors for filtering

Template method for descending classes to modify selectors if needed

## Usage

~~~~~
// basic usage
$result = $pageArray->filterDataSelectors($selectors);

// usage with all arguments
$result = $pageArray->filterDataSelectors(Selectors $selectors);
~~~~~

## Arguments

- `$selectors` `Selectors`
