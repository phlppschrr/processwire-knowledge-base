# $pagefiles->setPage(Page $page)

Source: `wire/core/Pagefiles.php`

Set the Page these files are assigned to

## Usage

~~~~~
// basic usage
$result = $pagefiles->setPage($page);

// usage with all arguments
$result = $pagefiles->setPage(Page $page);
~~~~~

## Arguments

- `$page` `Page`
