# $fileLog->__construct($path, $identifier = '')

Source: `wire/core/FileLog.php`

Construct the FileLog

## Arguments

- string $path Path where the log will be stored (path should have trailing slash) This may optionally include the filename if you intend to leave the second param blank.
- string $identifier Basename for the log file, not including the extension.
