# $templateFile->__toString(): string

Source: `wire/core/TemplateFile.php`

The string value of a TemplateFile is its PHP template filename OR its class name if no filename is set

## Usage

~~~~~
// basic usage
$string = $templateFile->__toString();
~~~~~

## Return value

- `string`
