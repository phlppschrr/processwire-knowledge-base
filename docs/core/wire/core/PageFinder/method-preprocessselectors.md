# $pageFinder->preProcessSelectors(Selectors $selectors, $options = array())

Source: `wire/core/PageFinder.php`

Pre-process given Selectors object

## Usage

~~~~~
// basic usage
$result = $pageFinder->preProcessSelectors($selectors);

// usage with all arguments
$result = $pageFinder->preProcessSelectors(Selectors $selectors, $options = array());
~~~~~

## Arguments

- `$selectors` `Selectors`
- `$options` (optional) `array`
