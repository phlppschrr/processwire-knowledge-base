# $inputfield->name($name = null): string|Inputfield

Source: `wire/core/Inputfield.php`

Get or set the name attribute. @since 3.0.110

## Usage

~~~~~
// basic usage
$string = $inputfield->name();

// usage with all arguments
$string = $inputfield->name($name = null);
~~~~~
