# $inputfield->skipLabel($skipLabel = null): int|Inputfield

Source: `wire/core/Inputfield.php`

Get or set the skipLabel constant property via method. @since 3.0.110

## Usage

~~~~~
// basic usage
$int = $inputfield->skipLabel();

// usage with all arguments
$int = $inputfield->skipLabel($skipLabel = null);
~~~~~
