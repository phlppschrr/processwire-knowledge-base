# $inputfield->icon($icon = null): string|Inputfield

Source: `wire/core/Inputfield.php`

Get or set the 'icon' property via method. @since 3.0.110

## Usage

~~~~~
// basic usage
$string = $inputfield->icon();

// usage with all arguments
$string = $inputfield->icon($icon = null);
~~~~~
