# $inputfield->requiredLabel($requiredLabel = null): string|Inputfield

Source: `wire/core/Inputfield.php`

Get or set the 'requiredLabel' property via method. @since 3.0.110

## Usage

~~~~~
// basic usage
$string = $inputfield->requiredLabel();

// usage with all arguments
$string = $inputfield->requiredLabel($requiredLabel = null);
~~~~~
