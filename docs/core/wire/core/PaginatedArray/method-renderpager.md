# $paginatedArray->renderPager(array $options = array()): string

Source: `wire/core/PaginatedArray.php`

Renders pagination, when MarkupPageArray module installed

## Usage

~~~~~
// basic usage
$string = $paginatedArray->renderPager();

// usage with all arguments
$string = $paginatedArray->renderPager(array $options = array());
~~~~~
