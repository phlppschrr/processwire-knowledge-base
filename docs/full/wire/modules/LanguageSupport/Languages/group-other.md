# Languages: other

Source: `wire/modules/LanguageSupport/Languages.php`

- $default: Language Get default language
- [$getDefault: Language](method-getdefault.md) Get default language (alias of $default)
- @method added(Page $language) Hook called when Language is added added(Page $language) Hook called when Language is added
- @method deleted(Page $language) Hook called when Language is deleted deleted(Page $language) Hook called when Language is deleted
- @method updated(Page $language, $what) Hook called when Language is added or deleted updated(Page $language, $what) Hook called when Language is added or deleted
- @method languageChanged($fromLanguage, $toLanguage) Hook called when User language is changed languageChanged($fromLanguage, $toLanguage) Hook called when User language is changed
