# $commentList->renderItemReady(Comment $comment, $depth): void

Source: `wire/modules/Fieldtype/FieldtypeComments/CommentList.php`

Declared in class docblock.

## Usage

~~~~~
// basic usage
$commentList->renderItemReady($comment, $depth);

// usage with all arguments
$commentList->renderItemReady(Comment $comment, $depth);
~~~~~
