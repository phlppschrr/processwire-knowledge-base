# $roles->added(Page $page): void

Source: `wire/core/Roles.php`

Hook called just after a Role is added

## Usage

~~~~~
// basic usage
$roles->added($page);

// usage with all arguments
$roles->added(Page $page);
~~~~~
