# $roles->saveReady(Page $page): array

Source: `wire/core/Roles.php`

Hook called just before a Role is saved

## Usage

~~~~~
// basic usage
$array = $roles->saveReady($page);

// usage with all arguments
$array = $roles->saveReady(Page $page);
~~~~~
