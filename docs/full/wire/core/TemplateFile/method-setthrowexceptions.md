# $templateFile->setThrowExceptions($throwExceptions)

Source: `wire/core/TemplateFile.php`

Call this with boolean false to disable exceptions when file doesn’t exist

## Arguments

- `$throwExceptions` `bool`
