# $wireInputData->string($varName): string

Source: `wire/core/WireInputData.php`

Return a value guaranteed to be a string, regardless of what type $varName is. Does not sanitize.

## Usage

~~~~~
// basic usage
$string = $wireInputData->string($varName);
~~~~~
