# $inputfieldWrapper->prepend(Inputfield $item): $this

Source: `wire/core/InputfieldWrapper.php`

Prepend an Inputfield to this instance’s children.

## Usage

~~~~~
// basic usage
$result = $inputfieldWrapper->prepend($item);

// usage with all arguments
$result = $inputfieldWrapper->prepend(Inputfield $item);
~~~~~

## Arguments

- `$item` `Inputfield` Item to prepend

## Return value

- `$this`
