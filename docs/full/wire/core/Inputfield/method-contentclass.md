# $inputfield->contentClass($class = null): string|Inputfield

Source: `wire/core/Inputfield.php`

Get content class attribute or add a class to it. @since 3.0.110

## Usage

~~~~~
// basic usage
$string = $inputfield->contentClass();

// usage with all arguments
$string = $inputfield->contentClass($class = null);
~~~~~
