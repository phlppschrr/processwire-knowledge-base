# $pagefile->filename(): string

Source: `wire/core/Pagefile.php`

Returns the full disk path name filename to the Pagefile.

## Usage

~~~~~
// basic usage
$string = $pagefile->filename();
~~~~~

## Return value

- `string`
