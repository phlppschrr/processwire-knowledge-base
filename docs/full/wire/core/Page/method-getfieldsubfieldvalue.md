# $page->getFieldSubfieldValue($key): mixed|null

Source: `wire/core/Page.php`

If given a field.subfield string, returns the associated value

This is like the getDot() method, but with additional protection during output formatting.

## Usage

~~~~~
// basic usage
$result = $page->getFieldSubfieldValue($key);
~~~~~

## Arguments

- $key

## Return value

- `mixed|null`

## Deprecated

Method no longer needed
