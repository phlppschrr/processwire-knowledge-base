# $page->publishable(): bool

Source: `wire/core/Page.php`

Returns true if the page is publishable by the current user, false if not.

## Usage

~~~~~
// basic usage
$bool = $page->publishable();
~~~~~
