# $pageFinder->getLimit(): int

Source: `wire/core/PageFinder.php`

Returns the limit placed upon the last find() operation, or 0 if no limit was specified

## Usage

~~~~~
// basic usage
$int = $pageFinder->getLimit();
~~~~~

## Return value

- `int`
