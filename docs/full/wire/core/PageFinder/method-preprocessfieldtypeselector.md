# $pageFinder->preProcessFieldtypeSelector(Selectors $selectors, Selector $selector)

Source: `wire/core/PageFinder.php`

Pre-process a selector having field name that begins with "Fieldtype"

## Arguments

- Selectors $selectors
- Selector $selector
