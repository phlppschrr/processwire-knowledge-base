# ModulesLoader::loadPath()

Source: `wire/core/ModulesLoader.php`

Given a disk path to the modules, determine all installed modules and keep track of all uninstalled (installable) modules.

@param string $path
