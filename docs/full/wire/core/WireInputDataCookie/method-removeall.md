# $wireInputDataCookie->removeAll(): $this|WireInputData

Source: `wire/core/WireInputDataCookie.php`

Remove all cookies (other than those required for current session)

## Usage

~~~~~
// basic usage
$result = $wireInputDataCookie->removeAll();
~~~~~

## Return value

- `$this|WireInputData`
