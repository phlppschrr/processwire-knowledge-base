# $wire->fields($name = ''): Field|Fields|null

Source: `wire/core/Wire.php`

Access the $fields API variable as a function.

## Usage

~~~~~
// basic usage
$field = $wire->fields();

// usage with all arguments
$field = $wire->fields($name = '');
~~~~~
