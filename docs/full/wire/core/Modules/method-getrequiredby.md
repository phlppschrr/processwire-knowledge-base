# $modules->getRequiredBy($class, $uninstalled = false, $installs = false)

Source: `wire/core/Modules.php`

**********************************************************************************
DEPENDENCIES

## Usage

~~~~~
// basic usage
$result = $modules->getRequiredBy($class);

// usage with all arguments
$result = $modules->getRequiredBy($class, $uninstalled = false, $installs = false);
~~~~~
